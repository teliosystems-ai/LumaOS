
I want to see the feasibility of the following: to create a kernel and OS similar to Linux which is an LLM based OS but with the following changes: 1- kernel is totally build using LLM skills. Every component is basically a skill to handle different hardware components of the host environment: CPU, I/O, Memory, storage, graphics and so on. 2- Services layer that handles a file system and applications with embedded chat like interface. File system is completely handled by the LLM to organize files and storage. 3- All applications are runnable as flash services/agents so they are called upon the user request. This layer shall support existing used application through an interface layer if it still needed 4- Supports full multi-modal interactions. 5- Ability to create on the fly content: data, sheets, diagrams, animations or ven custom code to handle requests as needed.   The OS shall use an embedded LLM which is part of the full Kernel/OS image. Can you produce a technical description with details of this and how to develop it as a complete engineering requiredments with HLD, LLD and funcional requirements. Also, for such design what will be the ideal LLM size to use in terms of parameters so it can be run on a consumer computer with a GPU or a dedicated AI chip like: AMD Ryzen AI 400 & Max PRO Series, Snapdragon X2 Elite, Intel Core Ultra Series 2 & 3. Note that such OS can be isntalled directly on target computer with the required HW components to support its performance requirements. 

